package com.mojang.bugs.risa.application

import com.mojang.bugs.risa.domain.*

fun generateReport(ticketsForSnapshot: List<Pair<TicketId, TicketTitle>>, currentSnapshot: Snapshot, previousSnapshots: Map<Snapshot, RedditPost>): Report {
    TODO()
}
